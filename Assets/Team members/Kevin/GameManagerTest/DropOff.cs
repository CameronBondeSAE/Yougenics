using System;
using System.Collections;
using System.Collections.Generic;
using Ollie;
using UnityEngine;

/*
namespace Kevin
{
    public class DropOff : MonoBehaviour
    {
        public K.GameManager gameManager;
        public EnergyContainer energyContainer;
        
        public void OnTriggerEnter(Collider other)
        {
            EnergyContainer otherEnergyContainer = other.GetComponent<EnergyContainer>();
            if (otherEnergyContainer != null)
            {
                energyContainer.EnergyUpdate();
            }
        }
        public void OnDrawGizmos()
        {
            Gizmos.DrawWireCube(transform.position,this.GetComponent<BoxCollider>().size);
        }
    }
}
*/
