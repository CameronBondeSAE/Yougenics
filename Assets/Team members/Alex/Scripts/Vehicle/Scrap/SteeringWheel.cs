using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SteeringWheel : MonoBehaviour
{
    /*
    public Rigidbody rb;
    void Update()
    {
        RaycastHit hitInfo;
        hitInfo = new RaycastHit();
        
        
        

        if (Physics.Raycast(transform.position, transform.TransformDirection(Vector3.left)))
        {
            Debug.DrawRay(transform.position, transform.TransformDirection(Vector3.left) * hitInfo.distance,
                Color.yellow);
            
            //force = maxForce - layerMask;
            Debug.Log("Did Hit Left");
            
        }
        else
        {
            Debug.DrawRay(transform.position, transform.TransformDirection(Vector3.left) * 1000, Color.white);
            Debug.Log("Did not Hit Left");
            //force = maxForce;
            
        }
        
        if (Physics.Raycast(transform.position, transform.TransformDirection(Vector3.right)))
        {
            Debug.DrawRay(transform.position, transform.TransformDirection(Vector3.right) * hitInfo.distance,
                Color.yellow);
            
            //force = maxForce - layerMask;
            Debug.Log("Did Hit Left");
            
        }
        else
        {
            Debug.DrawRay(transform.position, transform.TransformDirection(Vector3.right) * 1000, Color.white);
            Debug.Log("Did not Hit Left");
            //force = maxForce;
            
        }
    }
 */   
}
